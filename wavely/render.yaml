services:
  - type: web
    name: sonora
    runtime: node
    plan: free
    buildCommand: echo "no build step"
    startCommand: node --openssl-legacy-provider server.js
    healthCheckPath: /healthz
    envVars:
      - key: NODE_VERSION
        value: 20
